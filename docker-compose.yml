version: '2.2'

services:
  apt-cacher-ng:
    restart: always
    image: pdouble16/apt-cacher-ng:3.2-1
    init: true
    ports:
    - "3142:3142"
    volumes:
    - /srv/docker/apt-cacher-ng:/var/cache/apt-cacher-ng
